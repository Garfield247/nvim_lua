# Neovim 配置优化建议

## ✅ 已完成的优化

### 1. **修复硬编码路径问题**
- **问题**: `init.lua` 中硬编码了 `/Users/catman/.pyenv/shims/` 路径
- **修复**: 改为动态检测用户主目录和 pyenv 路径
- **影响**: 配置现在可以在不同用户之间共享

### 2. **修复已弃用的 LSP API**
- **问题**: `lspconfig.lua` 中使用了已弃用的 `vim.lsp.config()` API
- **修复**: 全部改为使用 `lspconfig.setup()` 标准 API
- **影响**: 避免未来版本兼容性问题，提高代码可维护性

### 3. **Vim 脚本转换为 Lua**
- **问题**: `autocmd.lua` 中使用了旧的 Vim 脚本语法
- **修复**: 全部转换为现代 Lua API
- **影响**: 更好的性能，更易维护，符合 Neovim 最佳实践

### 4. **统一键位映射方式**
- **问题**: `keymaps.lua` 中混合使用了 `vim.api.nvim_set_keymap` 和 `keymap.set`
- **修复**: 统一使用 `keymap.set`，并添加了所有键位的描述
- **影响**: 代码更一致，which-key 插件可以显示更好的帮助信息

### 5. **清理注释代码和错误注释**
- **问题**: `options.lua` 中有注释掉的代码和错误的注释说明
- **修复**: 清理了无用注释，修正了错误注释（如 "2 spaces" 实际是 4）
- **影响**: 代码更清晰，减少混淆

### 6. **修复 DAP 硬编码路径**
- **问题**: `dap.lua` 中使用了硬编码的 `~` 路径
- **修复**: 使用 `vim.fn.stdpath()` 动态获取路径，并添加了回退机制
- **影响**: 更好的跨平台兼容性

### 7. **更新 Mason LSP 服务器名称**
- **问题**: `mason.lua` 中使用了旧的 `ts_ls` 名称
- **修复**: 更新为 `typescript-language-server`，并添加了缺失的服务器
- **影响**: 确保所有配置的 LSP 服务器都能正确安装

## 📋 其他优化建议

### 性能优化

1. **延迟加载优化**
   - 考虑为大型插件添加更精确的 `event` 或 `ft` 条件
   - 例如：`telescope` 可以只在需要时加载

2. **LSP 性能**
   - 考虑为大型项目配置 `diagnosticMode = "workspace"` 的替代方案
   - 当前 `pyright` 使用 `"openFilesOnly"` 是好的选择

### 代码质量

1. **错误处理**
   - 在 `init.lua` 的 Python 路径检测中添加了错误处理
   - 建议在其他关键位置也添加类似的错误处理

2. **配置组织**
   - 考虑将 LSP 服务器配置拆分到单独的文件中（如 `lua/catman/plugins/lsp/servers/`）
   - 当前所有服务器在一个文件中，随着服务器增多会变得难以维护

### 功能增强建议

1. **LSP 配置**
   - 考虑添加 `vim.lsp.handlers` 的自定义配置以改善诊断显示
   - 可以添加 `vim.diagnostic.config()` 来统一诊断样式

2. **键位映射**
   - 考虑添加更多描述性键位，特别是插件相关的
   - 当前很多键位缺少 `desc` 字段（已部分修复）

3. **自动命令**
   - 可以考虑添加更多有用的自动命令，如：
     - 自动格式化特定文件类型
     - 自动保存时运行 linter
     - 文件类型特定的设置

### 安全性

1. **系统命令执行**
   - `ViewInBrowser` 函数直接执行系统命令，考虑添加验证
   - 确保文件路径是安全的

### 文档

1. **README 更新**
   - 建议更新 README.md 说明配置结构
   - 添加键位映射的完整列表

## 🎯 优先级建议

### 高优先级（已完成）
- ✅ 修复硬编码路径
- ✅ 修复已弃用 API
- ✅ 统一代码风格

### 中优先级（建议实施）
- 拆分 LSP 服务器配置到单独文件
- 添加更多错误处理
- 优化插件加载策略

### 低优先级（可选）
- 添加更多自动命令
- 完善文档
- 添加配置验证

## 📝 注意事项

1. **备份配置**: 在应用这些更改之前，建议备份原始配置
2. **测试**: 重启 Neovim 后测试所有功能是否正常
3. **插件更新**: 某些更改可能需要更新插件版本

## 🔍 检查清单

应用更改后，请检查：
- [ ] Python 路径是否正确检测
- [ ] 所有 LSP 服务器是否正常启动
- [ ] 键位映射是否正常工作
- [ ] 自动命令是否正常执行
- [ ] DAP 调试功能是否正常
